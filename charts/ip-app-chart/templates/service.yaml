apiVersion: v1
kind: Service
metadata:
  name: {{ include "ip-app-chart.fullname" . }}
  labels:
    {{- include "ip-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ip-app-chart.selectorLabels" . | nindent 4 }}
